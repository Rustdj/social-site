import './setting.module.css';
import classes from './setting.module.css';

const Setting = () => {
    return (
        <div className={classes.settingColor}>
            Setting
        </div>
    )
}

export default Setting;